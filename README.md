My config file for app on os lunux

Files:
    init.vim    -  config for NeoVim
    .vimrc      -  config for vim
    .tmux.conf  -  config for tmux
    install.ch  -  bash script for install (move) config files
